# Vietnamese AMR Parser - Environment Requirements
# Python 3.10+
# CUDA 12.1+
# Tested on: Quadro RTX 6000 (24GB VRAM)

# PyTorch ecosystem - Install with matching CUDA version
# For CUDA 11.8: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# For CUDA 12.1: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.3.0
torchvision>=0.18.0
torchaudio>=2.3.0

# Core transformers
transformers==4.46.3
accelerate==1.2.1
# NOTE: bitsandbytes REMOVED - Triton compatibility issues
# Use adamw_torch optimizer instead of adamw_8bit

# LoRA training
peft==0.13.2
trl==0.11.4

# Data processing
datasets>=2.14.0
pandas>=2.0.0
scikit-learn>=1.3.0

# AMR processing
penman>=1.2.0
smatch>=1.0.4

# Utilities
tqdm>=4.65.0
huggingface-hub>=0.24.0,<1.0  # IMPORTANT: transformers 4.46.3 requires <1.0
python-dotenv>=1.0.0

# NumPy - IMPORTANT: Keep at 1.x for compatibility
# NumPy 2.x breaks pandas/sklearn compiled with 1.x
numpy>=1.24.0,<2.0.0

# Optional: TensorBoard for monitoring
tensorboard>=2.15.0

# Note: xformers is optional, install separately if needed:
# pip install xformers --no-deps
